spring:
  datasource:
    url: jdbc:mysql://localhost:3306/itsc?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: Wc123123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update  # Options: none, update, create, create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect  # Changed from PostgreSQL to MySQL dialect

server:
  port: 8081
